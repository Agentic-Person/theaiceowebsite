# Supabase Configuration
# Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# N8N Webhook Configuration (for automated posting)
NEXT_PUBLIC_N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/blog-automation
N8N_WEBHOOK_SECRET=your_webhook_secret_key

# Chat API Configuration (existing)
NEXT_PUBLIC_CHAT_API_ENDPOINT=/api/chat

# GitHub Configuration (for MCP tools and API access)
GITHUB_TOKEN=your_github_personal_access_token

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual credentials
# 3. Never commit .env.local to version control
# 4. For GitHub token: Create at https://github.com/settings/tokens with repo access
